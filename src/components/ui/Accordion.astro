---
import ChevronIcon from "@/components/ui/icons/ChevronIcon.astro";
const { title, description, index } = Astro.props;
---

<div class="w-full max-w-3xl bg-white rounded-lg p-4 mb-2.5">
  <button
    onclick={`toggleAccordion(${index})`}
    class="w-full flex justify-between items-center cursor-pointer"
  >
    <span class="uppercase">{title}</span>
    <ChevronIcon
      id={`chevron-icon-${index}`}
      class="transition-transform duration-300 transform rotate-0"
    />
  </button>

  <div
    id={`content-${index}`}
    class="max-h-0 overflow-hidden transition-all duration-300 ease-in-out"
  >
    <div class="pt-4 text-left text-sm" set:html={description} />
  </div>
</div>
